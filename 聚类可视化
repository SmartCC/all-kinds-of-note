MDS（multidimensional scaling,MDS）是一个统计技术集合，用于可视化的描述距离集合中的相似性和差异性。对于经典的MDF，它的整个处理过程包括：输入一个包含数据集中任意两个数据点直接的距离的距离矩阵，返回一个坐标集合，这个集合可以近似反应每对数据点之间的距离。之所以说是近似反映，是因为在二维空间中很可能不存在被一组距离分开的点集。

函数 cmdscale(距离矩阵)
例子：
	ex.mds<-cmdscale(ex.dist)
	plot(ex.mds,type='n')
	text(ex.mds,c('A','B','C','D'))