1、docker pull 镜像名，获取相应的镜像

2、docker -t -i run 镜像名
  运行镜像，其中-t讲启动一个终端（terminal），-i时指定输入(input)

3、-v 宿主文件路径:docker文件路径，可以绑定一个宿主文件作为docker的文件，文件中的更改将立即反应到磁盘里。 注：-v的选项要尽量放在前面，不然会无法执行

4、docker提交更新步骤：
   （1)使用docker ps -l 查看修改容器的进程
    (2)使用docker commit dockerId docker容器：版本