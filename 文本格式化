1、windows格式文件在linux或者mac 下换行会出现样的情况，造成换行错误。
解决方案：
	使用vi或者sed等文本编辑工具。将内容替换。
		:%s/$//g # 去掉行尾的。
		:%s///g # 去掉所有的。
		:%s//[ctrl-v]+[enter]/g # 将替换成回车。
		:%s//\r/g # 将替换成回车。

注意：这里的""要使用"CTRL-V CTRL-M"生成，而不是直接键入“^M”。
